#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> A;
    for (int i = 0; i < 4; i++)
    {
        cin >> A[i];
    }
    sort(A, A + 4);
    int count = 0;


    return 0;
}